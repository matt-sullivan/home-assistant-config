lovelace:
  # Include the default and any other dashboards configured in the UI
  mode: storage
  # Add yaml dashboards
  dashboards:
    lovelace-yaml:
      mode: yaml
      title: Pool
      icon: mdi:pool
      show_in_sidebar: true
      filename: "ui-lovelace-pool.yaml"